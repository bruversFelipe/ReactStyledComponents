import React from "react";
import HeaderStyle from "./style";

const Header = () => (
  <HeaderStyle>
    <h1>
      Config Theme colors in <span>options</span>
    </h1>
  </HeaderStyle>
);

export default Header;
